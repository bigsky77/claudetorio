# Local development environment variables
# Copy to .env: cp .env.example .env

# === Database ===
POSTGRES_USER=claudetorio
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=claudetorio

# === Factorio ===
RCON_PASSWORD=dev_rcon_password
TOTAL_SLOTS=2
BASE_RCON_PORT=27000
BASE_UDP_PORT=34197
SERVER_HOST=localhost

# === Saves (writable local paths) ===
SAVES_DIR=./data/saves
FLE_SAVES_DIR=./data/fle-saves

# === Streaming ===
STREAM_BASE_URL=http://localhost
STREAM_BASE_PORT=3002
STREAM_PASSWORD=dev123

# === Optional ===
# ANTHROPIC_API_KEY=sk-ant-...
